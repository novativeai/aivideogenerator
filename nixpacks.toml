# Pin the NIXPACKS build to Python so a stray root package.json can never
# again make it auto-detect Node and skip installing Python (which left
# `python`/`hypercorn` missing at runtime). Deps come from requirements.txt.
providers = ["python"]
